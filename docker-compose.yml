services:
  checker:
    build: .
    image: uptime-checker:latest
    volumes:
      - ./logs:/logs
    command: ["https://www.google.com"]

  webapp:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html
